<h1>Slider creation</h1>

<form action="{{ path('auto_slider_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('auto_slider') }}">
            Back to the list
        </a>
    </li>
</ul>
