<h1>BotProduct list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Urlproduct</th>
            <th>Saveurl</th>
            <th>Checked</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('auto_botproduct_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.urlproduct }}</td>
            <td>{{ entity.saveurl }}</td>
            <td>{{ entity.checked }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('auto_botproduct_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('auto_botproduct_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('auto_botproduct_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
